version: '2'

services:
  postgres:
    image: postgres:16
    container_name: crybot_test_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: crybot_test
    ports:
      - "5432:5432"
    volumes:
      - ./data:/var/lib/postgresql/data