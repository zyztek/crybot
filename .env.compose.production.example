# Compose interpolation variables for production builds
# This file is loaded with `docker compose --env-file .env.compose.production`.

# Backend build argument
DATABASE_URL=postgresql://crybot:your-password@postgres:5432/crybot?schema=public

# Frontend build argument
VITE_API_URL=https://api.cryptofaucethub.com/api
